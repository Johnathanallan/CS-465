export interface Trip { // Define the Trip interface
  _id: string;
  code: string;
  name: string;
  length: number;
  start: Date;
  resort: string;
  perPerson: string;
  image: string;
  description: string;
  
}