import { AuthResponse } from './auth-response';
// Test suite for AuthResponse model
describe('AuthResponse', () => {
  it('should create an instance', () => {
    expect(new AuthResponse()).toBeTruthy();
  });
});
